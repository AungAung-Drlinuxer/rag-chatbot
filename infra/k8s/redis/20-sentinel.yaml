# === rag-chatbot — Redis Sentinel =========================================
# 3 sentinels form a Raft group, automatically elect a master after failover.
# Application connects via `redis-service` ClusterIP which the backend
# python-redis client resolves through sentinel topology discovery.

apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: redis-sentinel
  namespace: rag-chatbot
spec:
  serviceName: redis-sentinel-headless
  replicas: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: redis
      app.kubernetes.io/component: sentinel
  template:
    metadata:
      labels:
        app.kubernetes.io/name: redis
        app.kubernetes.io/component: sentinel
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                topologyKey: kubernetes.io/hostname
                labelSelector:
                  matchLabels:
                    app.kubernetes.io/name: redis
                    app.kubernetes.io/component: sentinel
      initContainers:
        - name: sentinel-config-init
          image: redis:7.4
          imagePullPolicy: IfNotPresent
          # Sentinel rewrites its config at runtime → needs a WRITABLE /conf.
          command: ["sh","-c","cp /readonly-config/sentinel.conf /conf/sentinel.conf && chmod 644 /conf/sentinel.conf"]
          volumeMounts:
            - { name: conf, mountPath: /conf }
            - { name: readonly-config, mountPath: /readonly-config }
      containers:
        - name: sentinel
          image: redis:7.4
          imagePullPolicy: IfNotPresent
          command:
            - redis-server
            - /conf/sentinel.conf
            - --sentinel
          ports:
            - name: sentinel
              containerPort: 26379
          volumeMounts:
            - { name: conf, mountPath: /conf }
          resources:
            requests: { cpu: 50m, memory: 64Mi }
            limits:   { cpu: 200m, memory: 128Mi }
          livenessProbe:
            exec:
              command: ["redis-cli","-p","26379","ping"]
            initialDelaySeconds: 30
            periodSeconds: 15
      volumes:
        - name: conf
          emptyDir: {}
        - name: readonly-config
          configMap:
            name: redis-config
            items: [{ key: sentinel.conf, path: sentinel.conf }]
---
apiVersion: v1
kind: Service
metadata:
  name: redis-sentinel-headless
  namespace: rag-chatbot
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: redis
    app.kubernetes.io/component: sentinel
  ports:
    - name: sentinel
      port: 26379
      targetPort: sentinel
---
apiVersion: v1
kind: Service
metadata:
  name: redis-sentinel-service
  namespace: rag-chatbot
spec:
  selector:
    app.kubernetes.io/name: redis
    app.kubernetes.io/component: sentinel
  ports:
    - name: sentinel
      port: 26379
      targetPort: sentinel
---
apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: rag-chatbot
spec:
  # Points at the redis DATA pods (the current master is discovered via Sentinel).
  selector:
    app.kubernetes.io/name: redis
    app.kubernetes.io/component: data
  ports:
    - name: client
      port: 6379
      targetPort: client
