apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: rag-chatbot
spec:
  selector:
    app.kubernetes.io/name: redis
    app.kubernetes.io/component: data
  ports:
    - name: redis
      port: 6379
      targetPort: client
